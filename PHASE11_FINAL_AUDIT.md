# PHASE11_FINAL_AUDIT.md

## نطاق هذه الجلسة — صريح منذ البداية

Phase 11 كما هو مكتوب يغطي 6 أولويات عبر كل شاشة تقريباً في التطبيق. لم أستطع تنفيذها كلها بعمق حقيقي في جلسة
واحدة. ما فعلته: تتبعت السبب الجذري لمشكلة العيادة كما طُلب حرفياً وأصلحتها، ثم استخدمت الوقت المتبقي في فحص
حقيقي (وليس نظري) لملفات المسار المالي — وهو ما كشف مشكلة أخطر مما وُصف في الطلب الأصلي. ما لم أصل إليه مذكور
بوضوح في القسم الأخير، وليس مخفياً.

**قيد مهم**: أدوات Supabase المباشرة (MCP) لم تكن متاحة في هذه الجلسة، تماماً كما حدث في الجلستين السابقتين. كل
إصلاح هنا مبني على قراءة الكود الفعلي الموجود في نفس هذا الـZIP (مطابقة استدعاءات RPC الموجودة بالفعل في
`finance.tsx`)، وليس افتراضاً لأسماء أعمدة أو دوال جديدة.

---

## الأولوية 1 — مشكلة شاشة العيادة (تم التتبع والإصلاح الفعلي)

### السبب الجذري
في `src/routes/_authenticated/clinic.tsx`، استعلام `visits` يحتوي `select` متداخل لجدول `patients` فيه:
- حقل `mrn` **مكرر مرتين**
- حقل `dob` **غير موجود فعلياً** في الجدول (الحقل الحقيقي هو `date_of_birth`، وهو مذكور بالفعل في نفس الـselect)

PostgREST يرفض أي طلب `select` يحتوي عمود غير موجود بخطأ 400 — أي أن **طلب تحميل قائمة العيادة بأكملها كان يفشل
دائماً**، بغض النظر عن الصلاحيات أو الـRoute أو التنقل. هذا ليس مشكلة صلاحيات ولا Route ولا Navigation — هي خطأ
استعلام صريح. `ErrorBox` كان يعرض الخطأ، لكنه يظهر كخطأ Postgres مبهم للمستخدم النهائي بدل قائمة المرضى.

### الإصلاح
- حذف الحقل المكرر `mrn` والحقل الوهمي `dob` من الـselect.
- إصلاح المرجع الاحتياطي `s(patient, "date_of_birth") || s(patient, "dob")` في حساب العمر — كان يعتمد جزئياً
  على نفس الحقل الوهمي.
- استبدال `<a href="/clinic/{id}">` (تنقل صفحة كاملة، Full Reload) بـ `<Link to="/clinic/$visitId">` من
  TanStack Router — نفس التعليق البرمجي القديم في الملف يوضح أن `<a>` استُخدم لتفادي مشكلة Radix Slot عند
  استخدام `<Button asChild><Link/></Button>`؛ استخدام `<Link>` مباشرة (بدون التفاف Button asChild) يتفادى نفس
  المشكلة مع الحصول على تنقل SPA حقيقي بدل إعادة تحميل الصفحة كاملة عند كل فتح لزيارة.

### فحص إضافي تم استبعاده
كنت قد افترضت في جلسة سابقة أن منطق حدود التاريخ (`gte`/`lt` على `visit_date`) قد يكون سبباً إضافياً بسبب فرق
التوقيت +02:00. بعد مطابقته مباشرة مع طريقة كتابة `visit_date` في `visits.tsx` (نفس الصيغة `.toISOString()` بنفس
الإزاحة +02:00 عند الحفظ)، تبين أن المنطق **متسق وصحيح فعلياً** — لم يكن هناك خطأ ثانٍ هنا كما توقعت سابقاً، وهذا
مذكور بصراحة بدل الاحتفاظ بافتراض غير مؤكد.

---

## New Visit من قائمة المرضى (تم التنفيذ)

- أُضيف عمود إجراء في `patients.tsx` بزر **"New Visit"** لكل مريض، يستخدم نفس آلية `?patient=<id>` الموجودة
  بالفعل في `visits.tsx` (والتي تفتح نافذة "زيارة جديدة" تلقائياً مع تحديد المريض مسبقاً) — لم أخترع آلية جديدة،
  أعدت استخدام واحدة موجودة ومُختبرة.
- تم التحقق: **Patient Profile** لديه بالفعل نفس الزر (من عمل سابق). **Search Results / Command Palette** لا
  يملك هذا الاختصار حالياً — لم ألمسه لأن الطلب في جلسة سابقة كان صريحاً بعدم إعادة كتابة بنية البحث، وإضافته
  يحتاج تعديل `search-service.ts` نفسه؛ هذا مذكور كتوصية للمستقبل بدل تنفيذ متسرع لملف أساسي.

---

## اكتشاف حقيقي وأخطر من المطلوب — تجاوز الـRPCs المالية الرسمية

أثناء فحص الأولوية 5 (Production QA)، وجدت أن **`src/routes/_authenticated/accounting.tsx`** هو شاشة مالية كاملة
موازية لـ`finance.tsx`، معروضة في التنقل (`/accounting` بصلاحية `accounting.read`)، لكنها تكتب مباشرة في الجداول
عبر `supabase.from(...).insert()` **بدل** الدوال الرسمية:
- إيداع/سحب يدوي من الخزنة → كتابة مباشرة في `cashbox_transactions` بدل `post_cashbox_entry()`
- تحصيل دفعة من مريض → كتابة مباشرة في `payments` بدل `post_invoice_payment()`
- فاتورة مورد جديدة → كتابة مباشرة في `supplier_bills` بدل `post_supplier_bill()`
- سداد فاتورة مورد → كتابة مباشرة في `supplier_payments` بدل `post_supplier_payment()`

هذا بالضبط ما طلبت التأكد من عدم وجوده: "لا يوجد أي مسار يخلق حركة مالية دون المرور عبر الـRPCs الرسمية." كان
موجوداً، وكان يعمل بشكل مستقل تماماً عن نظام الـledger الذري الذي بنته Phase 4 (بدون تحديث `paid_amount` على
الفاتورة، بدون idempotency، بدون توليد قيد ledger متسق).

### الإصلاح
أعدت توجيه جميع الكتابات الأربع في `accounting.tsx` لاستخدام نفس دوال RPC المستخدمة في `finance.tsx` بالضبط
(نفس التوقيعات، تم التحقق منها بمطابقة استدعاءات `finance.tsx` الفعلية سطراً بسطر، وليس تخميناً):
`post_cashbox_entry`، `post_invoice_payment`، `post_supplier_bill`، `post_supplier_payment`.

إنشاء مورد جديد (`suppliers.insert`) تُرك كما هو — بيانات أساسية (اسم/هاتف)، وليست حركة مالية.

### ما لم يُنفذ من هذا الاكتشاف
وجود شاشتين ماليتين متوازيتين (`/accounting` و `/finance`) في التنقل يبقى تكراراً معمارياً حقيقياً حتى بعد هذا
الإصلاح — كلاهما الآن آمن مالياً، لكن ما زال هناك احتمال ارتباك للمستخدم بين شاشتين لنفس الغرض. توحيدهما أو حذف
إحداهما قرار معماري أكبر لا أريد اتخاذه بتسرع ضمن هذه الجلسة — موصى به كعمل Phase 12.

---

## الأولوية 4 — PatientPicker (تم التحقق، لا يوجد عمل مطلوب)

فحصت كل الملفات المطلوبة: `visits.tsx`، `appointments.tsx`، `followups.tsx`، `billing.tsx`، `insurance.tsx`،
`certificates.tsx` — **كلها تستخدم `PatientPicker` بالفعل**. لا يوجد بحث مرضى يدوي أو `Select` قديم متبقٍ في أي
من هذه الملفات. هذا العنصر كان قد أُنجز فعلياً في جلسات سابقة (على الأرجح ضمن Phase 8–10 التي لم أشهدها مباشرة).

---

## الأولويات 2، 3، 6 — لم تُنفذ بعمق هذه الجلسة

- **الأولوية 2 (Workflow Audit الكامل)**: يحتاج تتبعاً يدوياً لعشرات الشاشات (Lab → Pharmacy → Billing →
  Insurance → Reports) — لم أستطع فعل هذا بنفس الدقة التي فعلتها لمشكلة العيادة ضمن الوقت المتاح.
- **الأولوية 3 (Navigation & UX)**: التقييم الصريح المطلوب — الواجهة الحالية (Nav Rail + Command Palette +
  kit.tsx بعد تحسينات الجلسات السابقة) وصلت لمستوى معقول من الاتساق البصري، لكنها **لم تصل بعد** لمستوى
  Linear/Raycast الحقيقي من ناحية الكثافة المعلوماتية وسرعة التفاعل (transitions/micro-interactions) — هذا تقييم
  صادق وليس تجميلياً. لم أنفذ تحسينات إضافية هذه الجلسة لأن ذلك يحتاج جلسة مخصصة كاملة، لا جزءاً من جلسة Hardening.
- **الأولوية 6 (Mobile First)**: لم أُجرِ فحصاً فعلياً لكل شاشة هذه الجلسة.

---

## نتيجة Build/Typecheck

- كل ملف معدّل تم فحصه فردياً بـ `tsc --strict` مطابقاً لإعدادات المشروع (`--jsx react-jsx --moduleResolution
  bundler` إلخ) — **نظيف، صفر أخطاء** على الملفات الثلاثة.
- **لم يُشغَّل `npm install` / `npm run typecheck` / `npm run build` الحقيقي** — لا توجد `node_modules` ولا
  اتصال شبكة في بيئة التنفيذ هذه. هذا نفس القيد في كل جلسة سابقة، مذكور بصراحة كما طلبت وليس معتبَراً "Passed".

---

## الملفات المعدّلة

1. `src/routes/_authenticated/clinic.tsx` — إصلاح استعلام العيادة (السبب الجذري) + تنقل SPA حقيقي
2. `src/routes/_authenticated/patients.tsx` — زر "New Visit" لكل صف مريض
3. `src/routes/_authenticated/accounting.tsx` — توجيه 4 عمليات كتابة مالية لاستخدام الـRPCs الرسمية بدل الكتابة
   المباشرة

لا migrations جديدة مطلوبة — كل الإصلاحات في هذه الجلسة على مستوى الواجهة فقط، تستخدم دوال RPC موجودة بالفعل.

---

## متابعة — اكتشاف إضافي في نفس عائلة مشكلة العيادة

بعد تتبع سلسلة Patient → Visit → Queue → Clinic فعلياً (وليس نظرياً)، تأكدت أن `visits.tsx` يُنشئ صفاً في
`queue_tickets` عند إنشاء الزيارة (سطر 305) — أي أن الربط Visit→Queue سليم فعلاً.

لكن زر **"Clinic"** في `queue.tsx` (نقطة الانتقال Queue→Clinic) كان يستخدم بالضبط نفس التركيب الذي وثّقه المطوّر
السابق كمصدر مشكلة في `clinic.tsx` نفسه: `<Button asChild><Link/></Button>`. بما أن هذا التركيب كان قد سبّب مشكلة
موثّقة صراحة في نفس المشروع لنفس الوجهة (`/clinic/$visitId`)، فمن المرجح جداً أن يكون هذا الزر — وليس فقط استعلام
`clinic.tsx` — هو ما واجهته فعلياً عند محاولة الوصول للعيادة من الطابور.

**الإصلاح**: استبدلته بـ`<Link>` مباشر باستخدام `buttonVariants({variant:"ghost",size:"sm"})` من نفس ملف
`button.tsx` الأصلي — نفس المظهر البصري تماماً، بدون الالتفاف بـ`Button asChild`.

**ما لم أفعله عمداً**: وجدت 9 استخدامات أخرى لنفس تركيب `Button asChild + Link` في `patients_.$patientId.tsx`
(5 مرات)، `lab.tsx`، `billing.tsx`، `lab_.$orderId.tsx`، و`kit.tsx` نفسه (المكوّن المشترك). **لم أُعِد كتابتها
جميعاً** — هذا التركيب هو الاستخدام القياسي والمعتاد لـshadcn/Radix ويعمل بشكل صحيح في الغالبية العظمى من
الحالات؛ إعادة كتابة 9 مواضع بناءً على تشابه النمط فقط، بدون دليل فعلي أن كل واحد منها معطوب، هو تخمين وليس
إصلاحاً — وقد يُدخل أخطاء جديدة في ملف مشترك (`kit.tsx`) بدل حلّ مشكلة حقيقية. التوصية: اختبار هذه الأزرار
التسعة يدوياً (نقرة فعلية على كل منها)، وإصلاح ما يثبت فعلاً أنه معطوب فقط.

## ملف إضافي معدّل
4. `src/routes/_authenticated/queue.tsx` — إصلاح زر الانتقال من الطابور للعيادة (نفس عائلة مشكلة الأولوية 1)

---

## متابعة ثانية — تحقق فعلي من السلسلة الكاملة (وليس افتراضاً)

### 1. هل شاشة Clinic نفسها تُحمَّل بشكل صحيح بعد التنقل؟
تحققت من طلب `visits` في `clinic.tsx` بعد الإصلاح — نظيف، لا أعمدة وهمية. تحققت أيضاً من استعلام
`ConsultationPage.tsx` (الصفحة الفعلية التي تُفتح عند فتح زيارة من العيادة) — أيضاً نظيف تماماً (`date_of_birth`
صحيح، لا يوجد `dob`). **مؤكَّد: كلا الاستعلامين سليمان الآن.**

### 2. هل يمكن فتح مريض في Clinic من Queue بدون أخطاء؟
وجدت مشكلة ثالثة من نفس العائلة: زر **"العيادة" (رجوع)** داخل `ConsultationPage.tsx` نفسه كان يستخدم نفس تركيب
`Button asChild + Link` المعطوب. أصلحته بنفس الطريقة (`buttonVariants`). الآن السلسلة الكاملة
Queue→Clinic→(رجوع)→Clinic تستخدم تنقل SPA حقيقي في كل نقطة، لا صفحة معطوبة في المنتصف.

### 3. هل يُحافَظ على سياق الزيارة/المريض/حالة الطابور؟
نعم — تحققت من الكود مباشرة: `ConsultationPage.tsx` يحمّل `patient_id, doctor_id, department_id,
consultation_fee` والعلاقات (`patients`, `departments`, `users`) **في استعلام واحد** مرتبط بـ`visitId` من الـURL.
لا يوجد فقدان سياق بين الانتقالات.

### 4. هل إنهاء الزيارة السريرية يغذّي المسارات التالية فعلاً؟
**هنا وجدت فجوة حقيقية، وليست افتراضاً.** زر "إنهاء الزيارة" في `ConsultationPage.tsx` يُحدّث `visits.status` إلى
`"completed"` فقط. تحققت مباشرة من `billing.tsx` — **لا يحتوي أي إشارة لـ`visit_id` أو لحالة الزيارة إطلاقاً**، ولا
آلية `?patient=` للتعبئة المسبقة كما في `visits.tsx`. أي أن إنهاء الزيارة سريرياً **لا يغذّي شاشة الفوترة تلقائياً
بأي شكل** — الفاتورة تُنشأ بشكل منفصل تماماً، بإعادة البحث عن نفس المريض من الصفر، بدون نقل `consultation_fee` أو
أي سياق من الزيارة.

**لم أُصلح هذه الفجوة هذه الجلسة عمداً** — إصلاحها الصحيح يعني إضافة آلية `validateSearch` جديدة لملف
`billing.tsx` وربما زر "فوترة هذه الزيارة" في `ConsultationPage.tsx` ينقل `consultation_fee` كبند فاتورة تلقائي —
هذا منطق عمل جديد (business logic جديد)، وليس مجرد إصلاح تنقل. تنفيذه بتسرّع ضمن جلسة مراجعة نهائية على شاشة
مالية حية مخاطرة لا أراها مبررة. موصى به كأولوية Phase 12 واضحة.

---

## New Visit من Patients و Patient Profile — تأكيد نهائي

- **Patients list**: الزر موجود (أُضيف في الجزء السابق من هذه الجلسة).
- **Patient Profile**: الزر موجود مسبقاً (من عمل سابق)، تحققت من وجوده الآن مباشرة في الكود — يستخدم نفس آلية
  `?patient=<id>`.
- السلسلة الفعلية الآن: **Patients أو Patient Profile → (نقرة واحدة) → نافذة New Visit مفتوحة مسبقاً بالمريض
  محدداً → حفظ → الزيارة تُدخِل صفاً في `queue_tickets` تلقائياً (تحقّقت من `visits.tsx` سطر 305) → Queue → زر
  Clinic (مُصلَح الآن) → ConsultationPage.** هذه سلسلة قابلة للتحقق فعلياً بقراءة الكود، وليست افتراضاً.

---

## تقييم صريح — Navigation Rail / Dashboard / Command Palette / Mobile Nav

لم أُجرِ تعديلات إضافية على هذه الشاشات هذه الجلسة — ركّزت الوقت المتاح على التحقق الفعلي من سلسلة العيادة
لأنها كانت المشكلة التشغيلية الحقيقية المُبلَّغ عنها. التقييم الصادق بناءً على حالتها الحالية (بعد تحسينات الجلسات
السابقة على `kit.tsx`/`nav-rail.tsx`/`dashboard.tsx`):
- **Navigation Rail**: بنية سليمة (collapse سلس، مفضّلة، صلاحيات)، لكنها أقرب لتطبيق SaaS جيد التنظيم منها
  لكثافة Linear/Raycast الفعلية — تنقصها micro-interactions (مثل tooltips فورية عند collapse، أو انتقال بصري عند
  تبديل الصفحات).
- **Dashboard**: الأقسام المسمّاة (Executive/Clinical/Pending/Pharmacy/Insurance) أضافت تنظيماً حقيقياً، لكن
  التخطيط ما زال شبكة بطاقات تقليدية، وليس بالضرورة "لوحة تنفيذية" بالمعنى الذي يوحي به Linear.
  Command Palette: البنية الأساسية (Ctrl+K، بحث موحّد) قوية ومطابقة للنمط المطلوب من ناحية الوظيفة، لم أراجع
  التفاصيل البصرية الدقيقة (سرعة الفتح، حدّة التركيز، إلخ) هذه الجلسة.
- **Mobile Nav**: FAB والتحسينات السابقة (لمسات أكبر) خطوة صحيحة، لكن لم أُجرِ اختباراً فعلياً على شاشات متعددة
  هذه الجلسة.

**هذا تقييم صادق وليس رفضاً للعمل** — لم أُجرِ "تحسينات عشوائية" بدون أساس فعلي بسبب ضيق الوقت المتبقي في هذه
الجلسة تحديداً؛ كل الوقت المتاح ذهب للتحقق الفعلي والمُثبَت من سلسلة العيادة كما طلبت صراحة "تحقق فعلي، ليس
افتراضات".

## ملف إضافي معدّل
5. `src/features/clinic/consultation/ConsultationPage.tsx` — إصلاح زر الرجوع للعيادة (نفس عائلة مشكلة التنقل)

## يحتاج تدخلاً يدوياً منك
- **الفجوة الأهم**: ربط إنهاء الزيارة السريرية بشاشة الفوترة (`billing.tsx`) — موصى به كأولوية Phase 12 أولى،
  موضّح أعلاه لماذا لم يُنفَّذ الآن.
- **اختبار الأزرار التسعة** المذكورة أعلاه بنقرة فعلية — أخبرني بأي منها معطوب فعلياً لأصلحه بدل التخمين.
- قرار: هل تُبقي `/accounting` و`/finance` كشاشتين منفصلتين (الآن كلاهما آمن) أم تُدمجهما لاحقاً؟
- تشغيل `npm install && npm run typecheck && npm run build` فعلياً على جهازك للتأكد من عدم وجود أخطاء لم يكتشفها
  الفحص المعزول.
- مراجعة صلاحية `accounting.read` — إذا قررت الإبقاء على الشاشتين، تأكد أن من يملك هذه الصلاحية هو نفسه من يجب
  أن يملك `cashbox.create`/`payments.create` (لم أتحقق من تطابق هذا التوزيع، فقط أن الكتابة نفسها أصبحت آمنة).
